{
    "scripts": {
        "build": "echo \"dummy build\"",
        "build:app": "cd .. && yarn build",
        "deploy": "node build && now --prod",
        "stage": "node build && now"
    },
    "dependencies": {
        "@now/node": "^1.6.1",
        "@sveltech/ssr": "^0.0.9",
        "dynamodb": "^1.2.1",
        "fs-extra": "^8.1.0",
        "joi": "^14.3.1",
        "uuid": "^8.0.0"
    },
    "devDependencies": {
        "@types/dynamodb": "^1.2.0",
        "@types/joi": "^14.3.4",
        "@types/uuid": "^7.0.3"
    }
}